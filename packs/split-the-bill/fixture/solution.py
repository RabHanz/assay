def split_bill(total: str, ways: int) -> list[str]:
    """Split a bill between `ways` people so the shares add back up to the bill, to the penny.

    `total` is pounds with two decimals, e.g. "100.00". Returns `ways` shares, each pounds with
    two decimals. The shares sum to `total` exactly; no two shares differ by more than one
    penny; when uneven, the smaller shares come first.
    """
    raise NotImplementedError
