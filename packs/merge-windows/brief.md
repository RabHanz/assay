The file `solution.py` in your workspace contains `merge_windows`, which is wrong: it returns its
input unchanged.

Fix it so that it merges overlapping or touching inclusive integer windows `[start, end]` and
returns the merged windows sorted by start. `[1,4]` and `[4,5]` touch and merge to `[1,5]`.
An empty list returns an empty list.

Read the file first, then write the complete corrected file with `write_file`, then call `done`.
Standard library only.
