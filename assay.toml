# Repository defaults for assay. With these, "label an issue" is literally sufficient:
# an issue that names no pack gets this one, and no `models:` line means three free attempts.
default_pack = "packs/chore-recurrence"
